{
  "name": "quiz-gems",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "cd frontend && npm install && npm run build",
    "start": "npx serve -s frontend/dist/frontend -l 10000"
  },
  "dependencies": {
    "serve": "^14.2.4"
  }
}
